spring:
  application:
    name: user.data.provider
  datasource:
    url: jdbc:postgresql://localhost:5432/user_request_stats
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
github:
  api:
    url: https://api.github.com
    usersEndpoint: /users
